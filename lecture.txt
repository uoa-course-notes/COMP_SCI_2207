+ Creating databases in such a way that we won't need to modify it too many times in the future. We want to preserve the "integrity"" of the data ...




Referential Integrity (relationship integrity): 
+ Declaring foreign keys for a table to refer to another. We can define constraints such as "ON DELETE NULL",


Value Integrity 
+ Use check clauses (like a Boolean check).
+ UNIQUE (no rows are the same), NOT NULL, 
+ AUTO_INCREMENT 
+ PRIMARY KEY: UNIQUE and NOT NULL.


+ JOINING TABLES: 
- 


